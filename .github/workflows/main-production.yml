name: main-production

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  production-gate:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Lint
        run: make lint

      - name: Security checks (Bandit + pip-audit)
        run: make security

      - name: Test gates
        run: |
          make test-unit
          make test-integration
          make test-coverage

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.8

      - name: Terraform validate
        run: make terraform-validate

      # Deploy em AWS temporariamente desativado.
      # Descomente os passos abaixo quando a equipe decidir habilitar implementacao em nuvem.
      # - name: Configure AWS credentials (OIDC)
      #   if: ${{ vars.ENABLE_TF_APPLY == 'true' && secrets.AWS_ROLE_TO_ASSUME != '' }}
      #   uses: aws-actions/configure-aws-credentials@v4
      #   with:
      #     role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
      #     aws-region: us-east-1

      # - name: Terraform apply (production)
      #   if: ${{ vars.ENABLE_TF_APPLY == 'true' && secrets.AWS_ROLE_TO_ASSUME != '' }}
      #   run: |
      #     terraform -chdir=terraform init
      #     terraform -chdir=terraform apply -auto-approve \
      #       -var="project_name=data-platform" \
      #       -var="environment=production" \
      #       -var="aws_region=us-east-1" \
      #       -var="raw_bucket_name=data-platform-production-raw" \
      #       -var="loc_bucket_name=data-platform-production-loc" \
      #       -var="silver_bucket_name=data-platform-production-silver"

      - name: Skip deploy notice
        run: |
          echo "Deployment skipped by policy."
          echo "AWS apply steps are commented in .github/workflows/main-production.yml."
